import time.timeBetween
import time.dateOf
policy "filter content in collection"
permit
    action == "list books";
obligation
    {
        "type" : "filterBooksByAge",
        "age" : timeBetween(subject.birthday, dateOf(|<time.now>), "years")
    }
